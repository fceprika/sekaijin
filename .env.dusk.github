APP_NAME=Sekaijin
APP_ENV=testing
APP_KEY=base64:n3kmRAHl1ll1WBOoKpd7BdA/syieRVufdOwRP6Drr0c=
APP_DEBUG=false
APP_URL=http://127.0.0.1:8000

# Logging optimized for CI
LOG_CHANNEL=stderr
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database configuration for GitHub Actions
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sekaijin_dusk
DB_USERNAME=root
DB_PASSWORD=root

# External services disabled for tests
MAPBOX_ACCESS_TOKEN=
GOOGLE_ANALYTICS_ID=
TINYMCE_API_KEY=

# Performance optimized for CI
BROADCAST_DRIVER=log
CACHE_DRIVER=array
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
SESSION_LIFETIME=120

# Memory cache disabled for CI
MEMCACHED_HOST=127.0.0.1

# Redis disabled for CI
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail testing configuration
MAIL_MAILER=array
MAIL_HOST=
MAIL_PORT=
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=
MAIL_FROM_ADDRESS="test@sekaijin.fr"
MAIL_FROM_NAME="${APP_NAME}"

# AWS disabled for tests
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher disabled for tests
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Vite configuration for CI
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Maintenance mode disabled
MAINTENANCE_MODE=false

# Dusk configuration for CI
DUSK_DRIVER_URL=http://localhost:9515
CI=true
GITHUB_ACTIONS=true

# Performance optimizations for CI
VIEW_CACHE_PATH=/tmp/view-cache
COMPILED_VIEW_PATH=/tmp/view-cache